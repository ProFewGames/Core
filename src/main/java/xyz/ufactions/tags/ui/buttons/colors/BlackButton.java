package xyz.ufactions.tags.ui.buttons.colors;

import org.bukkit.ChatColor;

import xyz.ufactions.tags.TitleModule;

public class BlackButton extends IColorButton {

	public BlackButton(TitleModule plugin) {
		super(plugin, ChatColor.BLACK, 1);
	}
}